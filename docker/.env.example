# PKG Docker Environment Variables
# Copy to .env and fill in actual values
# NEVER commit .env file!

# ==========================================
# DATABASE (Remote PostgreSQL)
# All credentials are REQUIRED
# ==========================================
DB_HOST=your-db-host.example.com
DB_PORT=5432
DB_USERNAME=your-db-username
DB_PASSWORD=your-db-password
DB_DATABASE=pkg
DB_SSL=true

# ==========================================
# REDIS
# ==========================================
REDIS_PREFIX=pkg:bull

# ==========================================
# API & SECRETS
# ==========================================
API_KEY=your-api-key
OPENAI_API_KEY=sk-your-openai-api-key

# ==========================================
# JWT AUTHENTICATION (REQUIRED)
# ==========================================
# Secret key for signing JWT tokens (min 32 characters recommended)
# Generate with: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-min-32-chars
# Access token expiration (default: 15m)
JWT_ACCESS_EXPIRATION=15m
# Refresh token expiration (default: 7d)
JWT_REFRESH_EXPIRATION=7d

# ==========================================
# TELEGRAM (optional - only for telegram-adapter)
# ==========================================
# GramJS userbot - for message collection
# Get credentials at https://my.telegram.org
TELEGRAM_API_ID=your-api-id
TELEGRAM_API_HASH=your-api-hash
TELEGRAM_SESSION_STRING=your-session-string

# Telegraf bot - for Second Brain commands (/recall, /prepare)
# Create bot via @BotFather in Telegram, get token
TELEGRAM_BOT_TOKEN=your-bot-token
# Comma-separated Telegram user IDs allowed to use the bot
# Get your ID from @userinfobot in Telegram
TELEGRAM_BOT_ALLOWED_USERS=123456789
# Allowed Telegram user IDs for Mini App auth
ALLOWED_TELEGRAM_IDS=123456789

# ==========================================
# TELEGRAM MINI APP
# ==========================================
# Mini App URL (used for web_app buttons in messages)
MINI_APP_URL=https://your-mini-app-domain.example.com

# ==========================================
# N8N
# ==========================================
# Deprecated in n8n 2.0+ (User Management is used instead)
# Kept for backwards compatibility
N8N_USER=admin
N8N_PASSWORD=your-secure-n8n-password

# n8n URLs (update with your domain)
N8N_EDITOR_BASE_URL=https://pkg.example.com/n8n/
WEBHOOK_URL=https://pkg.example.com/webhook/
TIMEZONE=Europe/Moscow

# ==========================================
# CLAUDE CLI (for LLM tasks)
# ==========================================
# Path to Claude credentials on host (after `claude login`)
# Default: ~/.claude (Linux) or leave empty for macOS Keychain
CLAUDE_CREDENTIALS_PATH=~/.claude
